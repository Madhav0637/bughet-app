<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.BudgetApp" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- See-through window, so the quick-entry pop-up floats over whatever app is on screen. -->
    <style name="Theme.BudgetApp.QuickEntry" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
